# Fastfetch branding

`config.jsonc` uses the existing ABS text logo and real upstream OS, kernel,
window-manager and hardware modules. It reports Debian truthfully and never
changes `/etc/os-release`. Both files are deployed into the user's configuration;
the logo source is the system-owned copy under `/usr/share/abs`.
