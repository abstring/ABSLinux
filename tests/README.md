# Tests

Tests will cover bootstrap idempotency, configuration validation, capability parsing, and profile composition. Hardware-dependent tests should use captured, sanitized fixtures rather than hostnames or machine-local secrets.
