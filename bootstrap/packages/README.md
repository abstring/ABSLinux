# Package manifests

Future package manifests will group Debian packages by purpose or capability. Package names should be kept separate from installation logic so the selection is reviewable, testable, and composable. No installable manifest exists yet.
